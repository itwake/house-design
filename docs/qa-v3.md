# V3 制作与检查记录

日期：2026-09-07。此记录说明检查范围，不构成施工验收。

## 模型与平面

- 平面和Blender均以`models/design-data.json`为坐标来源，manifest保存该文件SHA-256。
- 8个室内区域无采样重叠；7个内部开口在门宽10%、25%、50%、75%、90%位置的两侧均连通正确房间。
- 家具占地按9点采样检查，未跨越房间边界。检查不是门扇、抽屉及人体活动的完整碰撞模拟。
- 北侧次卧门厅、书房凹口和主卧门洞根据参考图拓扑修正，推算尺寸在`geometry-v3.md`中明确分级。
- V3.0.2 GLB包含1059个网格、3张内嵌表面贴图，约10.81MB；初版全量法线检查无NaN、零向量或异常长度。

### V3.0.1 书房南侧实墙回归

- 按用户圈图补上`w_bed_c_south`，南界`y=626,x=6..319 cm`连续，南墙没有门窗；东侧850mm门保留且5/5采样正确连通。
- 校验`wallSpecs`与`walls`逐项索引对应；新墙追加为索引20，不改变原外墙剖视规则。
- GLB检查POSITION数据的局部包围盒及节点/父节点变换，确认新增整墙世界范围为`[2.06,0,6.20]..[3.19,2.70,6.32] m`；低矮踢脚线不能冒充整墙通过检查。
- 内存副本模拟删墙、南墙误开门、索引失配、GLB标签正确但位置偏移20cm，四种错误均被回归检查检出。
- 前端数据、模型、渲染图和下载链接使用3.0.1资源版本，避免浏览器复用修正前的资产。
- 客厅、餐厅、书房和全屋4个视角已由修正后的同一Blender场景重渲（1200×800，Cycles 16采样及降噪），并逐张查看；其余6张保留，房间几何未变。
- 桌面平面、桌面3D及手机3D实际加载通过；无运行异常、回退状态、可见坏图或横向溢出。

## 实际画面检查引发的修正

### V3.0.2 三处飘窗回归

- 仅主卧北、次卧B北、客厅西三处改为飘窗。保留原墙洞位置、净宽、家具、房间多边形和上一版书房南侧实墙。
- 3D前窗框及玻璃在世界坐标北`z=-0.60 m`、西`x=1.40 m`；从原外墙外表面至前框中心暂定外挑600mm。前框最外缘北`z=-0.65 m`、西`x=1.35 m`与平面对应。
- 实际GLB部件校核覆盖前框、玻璃、侧返边、顶底板、深窗台和室内窗帘，并检查旧墙面无残留玻璃封住窗腔。全屋包络包含外凸，但房间面积未扩大。
- 内存模拟向内凸、误用墙中心起算投影、改动面积、旧包络裁切、原墙面残留玻璃、前框错位六类错误，均被检查检出。单独执行开口合并函数，验证共享数据优先于旧覆盖值且无重复开口。
- 平面、设计说明及SVG导出明确飘窗存在已确认，尺寸与侧面构造仍待复尺；600mm外凸、900mm窗台和实返边均为暂定示意。
- 桌面平面、桌面3D与手机3D载入正常，无可见坏图、运行异常、模型回退或横向溢出。前端资源版本3.0.2。
- 2026-09-08完成客厅、餐厅、主卧、次卧B及全屋5张同模型渲染（1200×800，Cycles 16采样及降噪），逐张核对外凸窗台；另外5张房间视角保留。未涉及的11个开口在manifest中的位置和竖向尺寸与上一版一致。

| 发现 | 修正 |
|---|---|
| 床尾毯与被子顶面共面导致黑面 | 毯底与被面分离2mm |
| 窗帘重叠、框角重叠导致黑条 | 窗帘折片留缝，门窗竖框与横框端部相接 |
| 床头侵入北窗下沿 | 主次卧床头降至880mm，窗台暂按900mm |
| 客卫镜子埋入墙内 | 移回室内侧 |
| 淋浴固定屏占满入口 | 主卫固定段600mm、客卫440mm，保留入口 |
| 两卫花洒遮窗 | 移到模型中东墙的北端实墙段，顶喷与窗投影分别留35/45mm；仍需复尺确认开窗及安装余量 |
| 洗烘木侧板误盖正面 | 木板移到北侧，露出白色机壳和门 |
| 阳台机位过近 | 退到客厅经现有门洞观察，不穿实墙、不新增开口 |

这些修正已同步到`build_blender.py`；`fix_blender_*.py`记录如何修补已有模型，无需正常重建时重复执行。

## 网页检查

- 10张1200×800的Blender实际渲染已逐张查看，覆盖全屋及9个空间视角；图片、GLB和Blender源文件的存在性检查通过。

- 桌面1440×1000、手机390×844均实际加载GLB，页面没有运行异常或横向溢出。
- 全屋、次卧聚焦、室内镜头、平面、效果图切换已操作检查。
- 手机初始镜头依据可用区域投影调整，完整户型不被标题和底部卡片裁切。
- 平面放大与SVG导出直接复用页面SVG，没有另一份几何坐标。
- 不再在V3数据失败时回退V2平面；加载失败明确提示。
- 合并静态网格、按需绘制、关闭实时阴影降低交互负载。镜面在交互中作轻量近似，真实反射看Blender渲染；二者几何一致。
- 餐厅面积不重复计算，公共区面积明确包含客厅、餐厅和过道。

## 仍需现场解决

墙体承重属性、梁柱和管井、窗高与开启方式、阳台封窗、厨房外窗、实际净高、设备检修净空、排水与电路均未完成现场设计。花洒避窗是在当前假设窗口内校核，不代表35/45mm余量足以直接施工。模型和效果图不能代替设计师复尺、施工图及物业审批。
